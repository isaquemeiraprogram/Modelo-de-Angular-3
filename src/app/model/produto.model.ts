// export = pra deixar acessivel = public
// interface= o que faz o codigo entender como uma especie de construtor da classe
export interface Produto {
    img: string
    nome: string
    preco: number
}

